package com.ttcintelligence.rippledetector;

public class Main {

    // Consumes the `service-alerts` topic, identifies downstream surface
    // routes affected by an upstream disruption (e.g. subway shuttle bus
    // replacements), and publishes cascade alerts to the `ripple-alerts` topic.
    public static void main(String[] args) {
    }
}
